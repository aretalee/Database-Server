package edu.uob;

import java.io.*;
import java.net.ServerSocket;
import java.net.Socket;
import java.nio.file.Paths;
import java.nio.file.Files;
import java.util.ArrayList;
import java.util.List;

public class Comparer {
    // equal to WHERE clause

    public static void filterTable(Table chosenTable, String chosenHeader, String value) {


        // maybe return array of IDs that match description?
        // cause if return filtered table then hard to DELETE

        // probably can use strcmp for String, Bool & ==
        // but need to convert for >= / <= / > / < (numbers)
        // also need to figure out AND + OR
        // LIKE (what is it?) -> search for pattern


    }

}
